trigger:
  batch: true
  branches:
    include:
      - main

pr:
  autoCancel: true
  branches:
    include:
      - main

pool:
  vmImage: 'ubuntu-latest'

jobs:
  - job: new
    steps:
      - script: env
        displayName: Show Environment
    strategy:
      matrix:
        one:
          some_var: A
        two:
          some_var: B
