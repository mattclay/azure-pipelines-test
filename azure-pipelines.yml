trigger:
  batch: true
  branches:
    include:
      - master

pr:
  autoCancel: true
  branches:
    include:
      - master

pool:
  vmImage: 'ubuntu-latest'

jobs:
  - job: init
    steps:
      - bash: ./init.py
        name: matrix
  - job: matrix
    dependsOn: init
    strategy:
      matrix: $[ dependencies.generator.outputs['init_matrix.config'] ]
    steps:
      - script: echo $(name)
